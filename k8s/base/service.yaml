apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  labels:
    app: mcp-server
    app.kubernetes.io/name: mcp-server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8181
      protocol: TCP
  selector:
    app: mcp-server
